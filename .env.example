# Optional: if you want to change ports or credentials, edit docker-compose.yml
# This file is only a reference and should NOT be committed with real secrets.

# Backend (example)
# DATABASE_URL=postgresql+psycopg2://todo:todo@db:5432/todo
# CORS_ORIGINS=http://localhost:3000

# Frontend (build-time in docker-compose)
# VITE_API_BASE_URL=http://localhost:8000/api/v1
